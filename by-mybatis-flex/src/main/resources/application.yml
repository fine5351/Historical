spring:
  application:
    name: by-mybatis-flex
  datasource:
    url: jdbc:mysql://localhost:3306/hackathon?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  main:
    banner-mode: off

server:
  port: 8080
