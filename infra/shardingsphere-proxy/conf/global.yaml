mode:
  type: Standalone
  repository:
    type: JDBC

authority:
  users:
    - user: root
      password: root
      admin: true
    - user: proxy
      password: proxy
  privilege:
    type: ALL_PERMITTED

props:
  system-log-level: debug
  proxy-default-port: 3307

