spring:
  main:
    banner-mode: off
  jmx:
    enabled: false

management:
  server:
    port: -1

nats:
  server-url: nats://localhost:4222

logging:
  level:
    root: INFO
    com.finekuo.nats: DEBUG
